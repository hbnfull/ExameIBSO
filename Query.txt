SELECT 
A.DNUM, A.DFECHA, A.DCANT, A.DCANTF, A.DPAR1, 
A.CLICOD, B.CLINOM, C.ICOD, D.IDESCR, C.AICANTF, 
C.AIALMACEN, D.ILISTA3, D.IFAM3, D.IFAM4, D.IFAM5
FROM FDOC A
INNER JOIN FCLI B ON A.CLICOD = B.CLICOD
INNER JOIN FAXINV C ON A.DNUM = C.FMOV
INNER JOIN FINV D ON C.ICOD = D.ICOD
WHERE A.DNUM = //Numero de Ticket//
;